class Player:
    def __init__(self, name, race, cls, atk, health):
        self.name = Hugo      # Store the player's name
        self.race = Vampire      # Store the player's race (e.g., human, elf, etc.)
        self.cls = Grim_Reaper        # Store the player's class (e.g., warrior, mage, etc.)
        self.atk = 666        # Store the player's attack power
        self.health = 1000 # Store the player's health points

class Weapon:
    def _init_ (self, name, category, damage):
        self.name = Ludwig
        self.category = Scythe
        self.damage = 1827

class Enemy:
    def _init_ (name, race, damage, health):
        self.name = Lycaon
        self.race = Wolf_Thiren
        self.damage = 
    


        

    # TODO: create the method to intialise Weapon object attributes (same as above)
    # The Weapon should have self, name, category and damage (can shorten if needed)
# TODO: Create an 'Enemy' class.

    # TODO: Create a method to initialise Enemy object attributes
    # Each enemy should have a name, race, damage and health.